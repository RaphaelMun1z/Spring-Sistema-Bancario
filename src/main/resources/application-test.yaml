spring:
  datasource:
    url: jdbc:h2:mem:sistemabancarioh2bd
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
      format_sql: true
cors:
  originPatterns: http://localhost:3000,http://localhost:8080